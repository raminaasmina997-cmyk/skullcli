{
  "name": "skullcli",
  "version": "1.3.3",
  "description": "A simple CLI that serves JSON hello over HTTP.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "skullcli": "./dist/server.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && cd client && npm install && npm run build && cd .. && mkdir -p dist/public && cp server.js dist/ && cp -r client/dist/* dist/public/ && cp package.json dist/",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/"
  ],
  "dependencies": {
    "express": "^4.19.2",
    "commander": "^12.1.0",
    "cors": "^2.8.5"
  },
  "keywords": [
    "cli",
    "http",
    "server",
    "json"
  ],
  "author": "TonNom",
  "license": "MIT"
}